services:
  - type: web
    name: pygame-multi-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/server.py
    envVars:
      - key: PYTHONPATH
        value: src
      - key: SDL_VIDEODRIVER
        value: dummy
      - key: SDL_AUDIODRIVER
        value: dummy
